name: Deploy staging

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to Contabo
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: 185.198.27.3
          username: root
          key: ${{ secrets.DEPLOY_SSH_KEY }}
          script: bash /opt/aymm/repo/deploy/deploy.sh
